config.stopBubbling = true
# Let @RequiredArgsConstructor-generated constructors keep @Qualifier so
# Spring can disambiguate injected beans (e.g. the portfolio task executor).
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
